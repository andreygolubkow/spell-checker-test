# MY SPELL PROJECT

# HOW TO RUN?

# QUESTIONS

1. Can we consider the input consists of A-Z, a-z, and single spaces, and '===' separator ? Should we support other characters? 
2. If we found unsupported character or unsupported sequence (like multiple spaces), should we return error or ignore it ? 
3. Is it okay to read input data from a file ? Should we support console input? 
4. Is it okay to write result to file ? Do we need console output? 
5. For case "Ignore any corrections that require two edits", does it mean we should write the word in this format {W?}